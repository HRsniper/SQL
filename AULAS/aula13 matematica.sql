SELECT numero FROM Usuarios LIMIT 1;

SELECT numero - 2 FROM Usuarios LIMIT 1;
SELECT numero + 2 FROM Usuarios LIMIT 1;
SELECT numero / 2 FROM Usuarios LIMIT 1;
SELECT numero * 2 FROM Usuarios LIMIT 1;
-- SELECT Sqrt(numero) FROM Usuarios LIMIT 1;

/* Abs Retorna o valor absoluto de um número
Atn Retorna o arco tangente de um número
Avg Retorna o valor médio de uma expressão
Cos Retorna o cosseno de um ângulo
Count Retorna o número de registros retornados por uma consulta selecionada
Exp Retorna e elevado à potência de um número especificado
Fix Retorna a parte inteira de um número
Format Formata um valor numérico com o formato especificado
Int Retorna a parte inteira de um número
Max Retorna o valor máximo em um conjunto de valores
Min Retorna o valor mínimo em um conjunto de valores
Randomize Inicializa o gerador de números aleatórios (usado por Rnd ()) com uma semente
Rnd Retorna um número aleatório
Round Arredonda um número para um determinado número de casas decimais
Sgn Retorna o sinal de um número
Sqr Retorna a raiz quadrada de um número
Sum Calcula a soma de um conjunto de valores
Val Lê uma string e retorna os números encontrados na string */